import React from 'react';
import TextBox from '../UI/TextBox/TextBox';

const Welcome = (props) => {
	return <TextBox title="Welcome! First things first.." subtitle="you can always change them later." />;
};

export default Welcome;
